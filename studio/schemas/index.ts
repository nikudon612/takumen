import blockContent from './blockContent'
import home from './home'
import navigation from './navigation'
import footer from './footer'

export const schemaTypes = [blockContent, navigation, home, footer]
